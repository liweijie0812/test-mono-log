name: flow-pilot

on:
  pull_request:
    branches: [main]
    types: [opened, edited]

jobs:
  flow:
    runs-on: ubuntu-latest
    steps:
      - uses: TDesignOteam/flow-pilot-action@develop
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
