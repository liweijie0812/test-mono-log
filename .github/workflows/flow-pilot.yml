name: flow-pilot

on:
  pull_request:
    branches: [main]
  pull_request_review:
    types: [edited]

jobs:
  flow:
    runs-on: ubuntu-latest
    steps:
      - uses: TDesignOteam/flow-pilot-action@develop
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
